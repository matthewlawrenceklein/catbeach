require "rails_helper"

RSpec.describe User, :type => :model do
#   before(:each) do
#     @user = User.new
#     @cat = Cat.new(user: @user)
#   end
#   # it {should have_one(:cat) } 
#  context "it has one and only one cat" do
#    it 'should have one cat' do
#     u = User.new
#     c = Cat.new(user: u)
#     expect (u.cat.).to eq(1)
#    end
#  end
end